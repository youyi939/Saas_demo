server:
  port: 9092

spring:

  application:
    name: nacos-payment-service                 #服务名
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848             #注册进nacos
      config:
        server-addr: 127.0.0.1:8848             #注册进nacos
        file-extension: yml

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


management:
  endpoints:
    web:
      exposure:
        include: '*'